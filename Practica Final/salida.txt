(setq a 0)  
(setq b 0)  
(defun main ()
	(setq c 3)  
	(setq a 1) 
	(setq b 2) 
	(print (+ (+ a b) c))  
	(setq a 0)  (setq b 2)  (setq c 0)  (setq d 0)  (setq e 4)  
	(setq f 2)  (setq g 0)  (setq h 0)  (setq i 5)  
) 
(main)
